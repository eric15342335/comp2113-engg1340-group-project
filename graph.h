#ifndef GRAPH_H
#define GRAPH_H
#include <string>
void graph_plotting(std::string player, unsigned int stocknum, int width, int height);
// input: stockname + width + height + stockname.log file
// return none
// output: print the graph of the stock price history
#endif